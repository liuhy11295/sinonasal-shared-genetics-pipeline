# 00 Environment setup

- `scripts/setup_environment.slurm`: creates and records the analysis environment.
- `scripts/install_method_tools.slurm`: installs external method dependencies.
- `scripts/create_input_manifest.sh`: creates the input symlink manifest.
